## Prerequisites

Install [Node.js](http://nodejs.org/) on your local machine.

## Environment Variables

These should be added to `.env` and to the GitHub repository as Secrets:

- `CAL_USERNAME` (for example: `rsefer`)
- `CAL_API_KEY` (Cal.com API key)

## Development

```
npm install
npm start
```
