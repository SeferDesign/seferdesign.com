<main>
	{% if caseStudy or product or legal %}
	<article>
		<h1>{{ title }}</h1>
		{{ content }}
	</article>
	{% elsif redirect %}
	<article>
		<h1>Redirecting...</h1>
	</article>
	{% else %}
	<article>
		<h1 class="seo-only">{{ title }}</h1>
		{{ content }}
	</article>
	{% endif %}
</main>
