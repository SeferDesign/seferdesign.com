<main>
	{% if tags contains 'caseStudy' or product or legal %}
	<article>
		<h1>{{ title }}</h1>
		{{ content }}
	</article>
	{% else %}
	<article>
		<h1 class="seo-only">{{ title }}</h1>
		{{ content }}
	</article>
	{% endif %}
</main>
